# Python / Django
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
backend/.env
backend/db.sqlite3
backend/media/
backend/staticfiles/
backend/venv/
venv/
.venv/

# Node / React
frontend/node_modules/
frontend/dist/
frontend/.env

# Divers
*.log
.DS_Store
Thumbs.db

# ── Matiere premiere, pas le site ──────────────────────────────────────────
# Les photos d'origine et les images de travail servent a FABRIQUER les
# visuels ; ce qui part en production, ce sont les fichiers decoupes de
# frontend/public/images/ et les medias televerses depuis l'Espace Gestion.
# shoes/ pese a lui seul 142 Mo : une fois dans l'historique, il y reste.
/shoes/
/*.png
/*.jpg
/*.jpeg
/*.webp

# Outils locaux de l'agent
.claude/

# Notes de travail
/REPRODUCE_*.md
/PIECES_SELECTIONNEES_ANALYSE.md
/GOLDEN_POUSSO_tasks.md

# Dossier de transit : les tableaux du hero, copies la pour etre televerses
# dans l'Espace Gestion. Les medias ne passent pas par git.
/hero-a-televerser/

# Les vidéos de l'« Aperçu de la boutique » NE SONT PAS servies par le site :
# elles se déposent dans Cloudflare et le site n'en garde que le lien (voir
# ShowcaseVideo dans CLAUDE.md). 58 Mo de .mp4 dans l'historique n'y
# serviraient personne, et n'en sortiraient plus.
/frontend/public/videos/

# Images de travail à la racine du front : les originaux des détourages de la
# vitrine, gardés pour refaire une découpe. Le site sert les .webp de
# frontend/public/images/.
/frontend/*.png
